apiVersion: v1
kind: Service
metadata:
  name: appname
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "appname"
    port: 8080
  selector:
    app: appname
